extends includes/layout

block content
    .page(itemscope itemtype='http://schema.org/CreativeWork')
          each article in site.posts.toArray()
              if article.academia
                  article!= article.content